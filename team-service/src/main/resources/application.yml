server:
  port: 8087
  forward-headers-strategy: framework

spring:
  application:
    name: TEAM-SERVICE

  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/team_db?createDatabaseIfNotExist=true
    username: root
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://${EUREKA_HOST:localhost}:8761/eureka

  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: "*"

  tracing:
    sampling:
      probability: 1.0

  zipkin:
    tracing:
      endpoint: http://${ZIPKIN_HOST:localhost}:9411/api/v2/spans

springdoc:
  api-docs:
    path: /v3/api-docs
  servers:
    - url: http://20.204.44.119:8085
      description: Azure Production
    - url: http://localhost:8085
      description: Local Development